CREATE TABLE IF NOT EXISTS EXPECTED_RELEASES (
    RELEASE_ID INTEGER PRIMARY KEY,
    MRELG_ID STRING,
    TITLE TEXT,
    ARTIST TEXT,
    LABEL_NAME TEXT,
    RELEASE_DATE DATE,
    GENRE STRING,
    EXPECTED_ALBUM_EQUIVALENT FLOAT,
    FW_STREAMS REAL DEFAULT 0,
    FW_SONGS REAL DEFAULT 0,
    FW_SALES REAL DEFAULT 0,
    SCENARIO TEXT,
    FY_VOL REAL,
    AVG_HISTORICAL_W1_PRODUCT_RATIO REAL,
    PRODUCT_RATIO_COEFFICIENT REAL,
    CLUSTER INTEGER
);
